@model NewDawn.Models.Permiso

@{
    ViewData["Title"] = "Eliminar Permiso";
}

<h1 class="text-uppercase text-danger">Eliminar Permiso</h1>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}

<h3 class="text-warning">¿Está seguro de que desea eliminar este permiso?</h3>

<div class="card p-3 shadow-sm">
    <h4>Detalles del permiso</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-3 font-weight-bold">
            @Html.DisplayNameFor(model => model.NombrePermiso)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.NombrePermiso)
        </dd>
        <dt class="col-sm-3 font-weight-bold">
            @Html.DisplayNameFor(model => model.DescripcionPermiso)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.DescripcionPermiso)
        </dd>
        <dt class="col-sm-3 font-weight-bold">
            @Html.DisplayNameFor(model => model.EstadoPermisos)
        </dt>
        <dd class="col-sm-9">
            @(Model.EstadoPermisos ? "Activo" : "Inactivo")
        </dd>
        <dt class="col-sm-3 font-weight-bold">
            @Html.DisplayNameFor(model => model.FechaCambio)
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.FechaCambio)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Idpermisos" />
        <div class="mt-3">
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>
